steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/order-to-cash-backend', '-f', 'Dockerfile.backend', '.']
images:
- 'gcr.io/$PROJECT_ID/order-to-cash-backend'
